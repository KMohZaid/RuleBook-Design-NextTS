'use client';

import FormComponent from './FormComponent';  // Import the reusable component

export default function CreatePageComponent() {
	return (
		<FormComponent
			actionText="Create"
		/>
	);
}

